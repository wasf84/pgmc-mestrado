#include <iostream>

using namespace std;

/**
Escreva uma função que receba como parâmetros quatro números inteiros (A, B, C e D)
    e retorne o valor 0 se os números digitados são válidos ou o valor 1 caso sejam inválidos.
    Eles serão válidos se todas as seguintes condições são satisfeitas:
    (a) B for maior que C;
    (b) D for maior que A;
    (c) a soma de C com D for maior que a soma de A com B;
    (d) C e D forem valores positivos; e
    (e) A é um número par.

Escreva uma função principal (main) que leia quatro valores inteiros digitados pelo usuário,
    chame a função anterior e imprima a mensagem "Valores aceitos" ou "Valores nao aceitos"
    de acordo com o retorno da função.
*/

int verificar(int a, int b, int c, int d) {
    int saida = -1;

    return saida;
}

int main() {
    cout << "Hello world!" << endl;
    return 0;
}
